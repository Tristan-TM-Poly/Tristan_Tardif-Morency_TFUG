# FRACTAL_LOOP_BOUNDED_EXAMPLE — Applied Snapshot (2026-04-14)

## Purpose
Create the first bounded example scaffold for Fractal-Loop so score/review/benchmark language can attach to a single honest object.

## Example skeleton
- object: one bounded Fractal-Loop instance
- scale: explicitly small
- time/depth horizon: explicitly finite
- observable family: explicitly limited
- success criterion: clarity increase without canon inflation

## Example outputs
1. one small instance description
2. one reduced trace pointer
3. one boundedness verdict
4. one clarity-gain verdict
5. one unresolved-risk note

## Rule
The bounded example must stay small enough that failure can still be informative.
